import 'package:flutter/material.dart';

class colerconstant {
  static String appMainName = 'DriveWay';
  static String appPowerBy = 'Powered By Malik';

  // Light theme colors
  static const appmaincolor = Color.fromARGB(255, 163, 29, 1);
  static const appsecondarycolor = Color.fromRGBO(236, 113, 3, 1);
  static const apptextcolor =Color.fromRGBO(251, 245, 244, 1); // Light text color
  static const appstatusbarcolor = Color.fromRGBO(251, 245, 244, 1);
  static const appfontcolor = Color.fromRGBO(74, 0, 0, 1);
  static const appmechatcolor = Color.fromRGBO(187, 35, 5, 1);
  static const appyouchatcolor = Color.fromARGB(223, 240, 204, 197);

  // Dark theme colors
  static const darkAppMainColor = Color.fromRGBO(163, 29, 1, 1); // Same color, can change if needed
  static const darkAppSecondaryColor = Color.fromRGBO(176, 0, 0, 1); // Same color, can change if needed
  static const darkAppTextColor = Colors.white; // Dark mode text color
  static const darkAppStatusBarColor = Colors.black; // Dark mode status bar
  static const darkAppFontColor = Colors.white; // Dark mode font color
  static const darkAppMechatColor = Color.fromRGBO(187, 35, 5, 1);
  static const darkAppYouChatColor = Color.fromARGB(223, 240, 204, 197); // Same color, can change if needed
}
